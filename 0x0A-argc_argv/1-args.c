#include <stdio.h>

/**
 * main - check the code
 * @argc:number of arguments
 * @argv:strings of arguments
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
